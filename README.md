# Merge Time Ranges

## 🧩 Overview
This Node.js module merges discontinuous or overlapping time ranges into sorted, non-overlapping intervals.  
If two ranges are separated by a small gap (less than or equal to a given threshold in milliseconds), they are treated as continuous and merged.

---

## ⚙️ Installation & Setup

### 1️⃣ Clone or download the folder
```bash
cd ~/merge-time-ranges
